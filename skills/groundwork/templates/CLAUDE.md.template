# <Project name> — Claude Code context

<!-- This file only exists when symlinks aren't available (Windows +
     core.symlinks=false, or `groundwork init` was answered "two files").
     AGENTS.md is the canonical source — keep this in parity by hand or via
     `groundwork document`. If your environment supports symlinks, prefer:
       rm CLAUDE.md && ln -sf AGENTS.md CLAUDE.md
     Aim under ~80 lines / ~400 tokens; when this file outgrows the budget,
     split per-area conventions into docs/agents/<area>.md and reference them
     from the "See also" section.

     Each section below has a distinct scope. Do NOT restate the same rule
     in two sections — the agent is reading the union, and duplication
     wastes attention. -->

## What this project is

<!-- One paragraph. Audience, problem, stack at a glance. -->

## Stack

- Language(s): <e.g. TypeScript 5.x>
- Package manager: <pkg-mgr>
- Runtime / framework: <e.g. Node 20, Astro 5>

## Verification

After any change:
```bash
<fast verification command(s)>
```

Before declaring a task done:
```bash
<full verification command(s)>
```

## Non-negotiables

<!-- Universal procedural rules that apply to every change. State each rule
     once, abstractly. Triggers and path-scoped actions belong in Boundaries. -->

- Use plan mode when any Ask-first condition triggers (see Boundaries).
- No placeholder comments. If you do not have enough context, ask.
- Use `<pkg-mgr>` only. Lockfiles are committed.
- Run the fast verification after every meaningful change.

## Style

<!-- 3 to 6 critical rules. Each with a Preferred / Avoid pair. Add more
     sections as needed. -->

### <Rule 1>
```<lang>
// Preferred
<code>
// Avoid
<code>
```

## Boundaries

<!-- Three tiers, each with strict scope. A rule belongs in exactly one. -->

**Always** — path- or area-scoped automatic actions (NOT universal rules; those are Non-negotiables).
- <path-scoped behavior>

**Ask first** — owns the trigger list for plan mode and other "stop and propose" gates.
- Changes touching >3 files, public APIs, refactors, or migrations.
- <project-specific trigger>

**Never** — forbidden paths or destructive actions.
- <path>: <reason>

## Preserved regions

Code wrapped in `<!-- preserve:start -->` / `<!-- preserve:end -->` must not be edited without explicit instruction.

## See also

- `docs/agents/` — per-area conventions when this file outgrows its budget
- `docs/decisions/` — ADRs
