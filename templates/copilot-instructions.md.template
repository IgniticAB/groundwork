# <Project name> — GitHub Copilot instructions

<!-- Loaded automatically by Copilot Chat in this repo. Mirror of CLAUDE.md and AGENTS.md.
     Keep all three in sync via context-engineer document. -->

## What this project is

<!-- One paragraph. -->

## Verification

After any change: `<fast command>`.

Before declaring a task done: `<full command>`.

## The non-negotiables

- Plan mode for changes touching more than 3 files, public APIs, refactors, or migrations.
- No placeholder comments.
- Use `<pkg-mgr>`.
- Run fast verification after every meaningful change.

## Style

### <Rule 1>
```<lang>
// Preferred

// Avoid
```

### <Rule 2>

## Out-of-bounds

- `<path>`: <reason>

## More

`.context/conventions.md` is the canonical source. ADRs in `docs/decisions/`.
